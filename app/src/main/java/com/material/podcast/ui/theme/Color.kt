package com.material.podcast.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Seed / swatch colors used by the in-app theme picker.
 *
 * The full Material 3 [androidx.compose.material3.ColorScheme]s are assembled in [Theme.kt];
 * these constants are only the representative "dot" shown in the theme chooser so the
 * palette is recognisable at a glance.
 */
val IndigoSeed = Color(0xFF4C5BD4)
val EmeraldSeed = Color(0xFF00696C)
val RoseSeed = Color(0xFFB23A48)
val AmberSeed = Color(0xFF8A6A00)
val LavenderSeed = Color(0xFF7B4DBC)

// Easter egg — dark gothic golden theme inspired by the Lands Between
// Rune-gold primary, void-black surfaces, bloodflame crimson tertiary
val EldenRingSeed = Color(0xFFD4A843)
